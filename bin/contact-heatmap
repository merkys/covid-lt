#!/usr/bin/Rscript --vanilla

file = '/dev/stdin'
files = commandArgs(trailingOnly=TRUE)
if( length(files) != 0 ) {
    file = files[1]
}

D = read.csv(file, header=TRUE, sep="\t", na.strings="?")
svg("/dev/stdout")
heatmap(t(as.matrix(D)), Colv=NA, Rowv=NA)
