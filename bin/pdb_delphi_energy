#!/bin/bash

delphi <(cat <<END
in(pdb,file="$1")
in(siz,file="delphi-parameters/amber.siz")
in(crg,file="delphi-parameters/amber.crg")

indi =  2.0
exdi = 80.0
salt =  0.0

maxc  = 0.0001
nonit = 800

energy(s,c,g)
END
)
