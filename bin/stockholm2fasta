#!/usr/bin/env python3

from Bio import SeqIO
import sys

files = sys.argv[1:]
if len(files) == 0:
    files = [sys.stdin]

for filename in files:
    SeqIO.convert(filename, 'stockholm', sys.stdout, 'fasta')
