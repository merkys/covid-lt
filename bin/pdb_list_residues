#!/usr/bin/env python3

# List residues in PDB files

from pdbio.pdbfile import PDBFile
import sys

for filename in sys.argv[1:]:
    pdb = PDBFile(filename)
    for chain in pdb:
        for residue in chain:
            print(chain.name, residue.number(), residue.icode())
